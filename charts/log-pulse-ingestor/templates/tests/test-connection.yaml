apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "log-pulse-ingestor.fullname" . }}-test-connection"
  labels:
    {{- include "log-pulse-ingestor.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "log-pulse-ingestor.fullname" . }}:{{ .Values.appConfig.grpc.port }}']
  restartPolicy: Never
